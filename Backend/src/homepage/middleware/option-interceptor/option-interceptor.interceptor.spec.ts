import { OptionInterceptorInterceptor } from './option-interceptor.interceptor';

describe('OptionInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new OptionInterceptorInterceptor()).toBeDefined();
  });
});
