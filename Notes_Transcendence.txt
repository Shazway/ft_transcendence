Notes Structures BDD:
users
	id
	mdp
	username
	nickname
	profile pic
	friends (pointeur sur user)
	matchs passes (pointeur sur matchs)
	rank
	status
	achievements
	blacklist
	channels


matchs
	id
	userA
	userB
	scoreA
	scoreB
	time
	ranked
	currently spectating
	settings (map, level ...)


channel
	id
	name
	profile pic
	visibility
	password
	owner
	admins
	members
	messages
	banned users
	muted users


messages
	id
	author
	channel
	date, hour
	content

muted users
	user
	time of end

Backend dependencies: (you have to be in the Backend file):
npm install @nestjs/cli && \
npm install class-transformer --save && \
npm install --save dotenv && \
npm install --save @nestjs/typeorm typeorm pg && \
npm install --save @nestjs/passport passport && \
npm install --save-dev @types/passport-local && \
npm install --save @nestjs/jwt passport-jwt

Frontend dependencies: (you have to be in the Frontend file): 
npm install <-- execute this if you do not have node_modules file locally in your Frontend file

BDD links:
https://hub.docker.com/_/postgres
https://suedbroecker.net/2020/06/23/run-a-postgressql-container-as-a-non-root-user-in-openshift/

Connexion BDD:
docker exec -it postgresql bash
psql -U root -W
psql -U inox -d inox_db -W
psql -a -f ./docker-entrypoint-initdb.d/Setup.sql

in sql mode:
\d to see all tables

Les commandes migrations sont casse pour le moment, pas toucher